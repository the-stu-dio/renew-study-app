# Renew Study Django App

This is a Django web application for the Renew Study project. It provides user accounts, study session tracking, and interactive content for research participants.

## 🚀 Quick Start

### 1. Create and activate a virtual environment
```bash
python3 -m venv .venv
source .venv/bin/activate   # macOS/Linux
```

### 2. Install dependencies
```bash
pip install -r requirements.txt
```

### 3. Start the development server
```bash
python3 manage.py runserver
```
